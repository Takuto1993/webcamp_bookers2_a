<p id="notice"></p>
<% if @book.errors.any? %>
 <h3><%= @book.errors.count %>errors prohibited this obj from being saved:</h3>
 <ul>
  <% @book.errors.full_messages.each do |message| %>
  <li><%= message %></li>
  <% end %>
 </ul>
<% end %>

     <div class="container px-5 px-sm-0">
      <div class="row">
       <div class="col-md-3">
        <%= render 'books/list', books: @booknew, user: @user %>
       </div>
       <div class='col-md-8 ofset-md-1'>
        <%= render 'books/bookindex', books: @books, user: @user %>
       </div>
      </div>
     </div>