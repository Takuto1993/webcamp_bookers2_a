<h2>User info</h2>

<h2>New Book</h2>
<%= form_with model: Book.new, url: '/books', method: :post do |f| %>
 <h4>Title</h4>
 <%= f.text_field :title %>
 
 <h4>Opinion</h4>
 <%= f.text_area :body %>
 
 <%= f.submit 'Creat Book' %>
<% end %>